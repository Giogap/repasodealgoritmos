#include <stdio.h>
#include "main.h"

/**
* main - imprimir producto o suma de  3 enteros
* Return: 0
*/

int main(void)
{
	int a, b, c;
	int sum;
    int produc;

	a = 8;
	b = 5;
	c = 4;

    if (a > 0)
    {
        printf("%d el prducto es\n", produc);
    }
    else
    {
        printf("%d la suma es\n", sum);
    }
	return (0);
}