#include <stdio.h>
#include "main.h"

/**
* main - imprimir producto o suma de  3 enteros
* Return: 0
*/

int main(void)
{
	int a, b, c;
	int suma;
    int producto;

	a = 8;
	b = 5;
	c = 4;

    if (a > 0)
    {
        printf("%d el prducto es\n", producto);
    }
    else
    {
        printf("%d la suma es\n", producto);
    }
	return (0);
}